void renderBanner() {
    final boolean a1=platform.toUpperCase().indexOf("MAC") > -1;
    final boolean a2=browser.toUpperCase().indexOf("IE") > -1;
    final boolean a3=wasInitialized();
    final boolean a4=resize > 0;
  if (a1 && a2 && a3 && a4)
  {
    // do something
  }
}